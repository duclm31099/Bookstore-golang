package event

const (
	RegisterEvent               = "user.registered"
	LoginEvent                  = "user.login"
	LogoutEvent                 = "user.logout"
	ResetPasswordRequestedEvent = "user.reset_password_requested"
	ResetPasswordCompletedEvent = "user.reset_password_completed"
)
